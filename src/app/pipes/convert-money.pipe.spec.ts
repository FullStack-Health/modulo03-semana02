import { ConvertMoneyPipe } from './convert-money.pipe';

describe('ConvertMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
